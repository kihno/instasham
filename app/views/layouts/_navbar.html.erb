<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to "Instasham", root_path, class:"navbar-brand" %>
    <%= form_tag(search_path, :method => "get", class: "d-flex") do %>
      <%= search_field_tag :search, params[:search], placeholder: "search", class: "form-control me-2" %>
      <%= button_tag "Search", class: "btn btn-outline-success", type: "submit" %>
    <% end %>
    <%# <form class="d-flex">
      <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success" type="submit">Search</button>
    </form> %>
    <div>
      <% if user_signed_in? %>
          <span>
            <%= image_tag current_user.avatar, style:"width: 50px; height: 50px" %>
            <%= link_to current_user.username, current_user, class: "nav-link" %>
          </span>
        <%= link_to "Sign Out", destroy_user_session_path, class:"nav-link" %>
      <% else %>
        <%= link_to "Sign Up", new_user_registration_path, class:"nav-link" %>
        <%= link_to "Sign In", new_user_session_path, class:"nav-link" %>
      <% end %>
    </div>
  </div>
</nav>
